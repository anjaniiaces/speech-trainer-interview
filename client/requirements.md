## Packages
lucide-react | Icons for the UI
framer-motion | Smooth animations and transitions
clsx | Class name merging
tailwind-merge | Class name merging

## Notes
- Using built-in browser Web Speech API (`window.SpeechRecognition` or `window.webkitSpeechRecognition`) for speech-to-text.
- Browser compatibility: Works best in Chrome/Edge. Will show a fallback message if unsupported.
- The UI defaults to a premium corporate dark mode aesthetic as requested.
